services:
  backend:
      image: backend:dev
      build: ../backend
      ports:
        - "8001:8000"
      environment:
        MODEL_NAME: weather-model
        DEV_ENV: True
  frontend:
      image: frontend:dev
      build: ../frontend
      environment:
        FLASK_BACKEND_NAME: backend-dev
        # this value is baked into the static build; browsers will use it
        VITE_BACKEND_URL: "http://localhost:8001"
      ports:
       - "8080:80"
  