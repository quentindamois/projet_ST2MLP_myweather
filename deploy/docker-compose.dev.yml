services:
  backend:
      build: ../backend
      ports:
        - "8001:8000"
      environment:
        MODEL_NAME: weather-model
        DEV_ENV: True
  frontend:
      build: ../frontend
      environment:
        FLASK_BACKEND_NAME: backend-dev
      ports:
       - "8080:80"
  